spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

jwt:
  secret: SmFydFNlY3JldEtleUZvclNtYXJ0Q1ZJQVBwbGljYXRpb24yMDI2RGV2
  expiration: 86400000

cors:
  allowed-origins: http://localhost:5173

gemini:
  api-key: test-key
  model: gemini-1.5-flash

app:
  seed-data: false
